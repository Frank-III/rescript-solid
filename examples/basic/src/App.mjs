// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Counter from "./Counter.mjs";
import * as Features from "./Features.mjs";
import * as SolidJSX from "rescript-solid/src/SolidJSX.mjs";
import * as SolidJs from "solid-js";
import * as CheckoutOptimistic from "./CheckoutOptimistic.mjs";
import * as JsxRuntime from "@solidjs/h/jsx-runtime";

function App(props) {
  let match = SolidJs.createSignal(true);
  let setShowCounter = match[1];
  let showCounter = match[0];
  let match$1 = SolidJs.createSignal([
    "Apple",
    "Banana",
    "Orange"
  ]);
  SolidJs.createTrackedEffect(() => {
    console.log("Counter visibility changed: " + (
      showCounter() ? "visible" : "hidden"
    ));
  });
  return <div>
    <h1>
      {"ReScript + Solid.js Example"}
    </h1>
    <div>
      <button
        onClick={param => setShowCounter(prev => !prev)}
      >
        {showCounter() ? "Hide Counter" : "Show Counter"}
      </button>
    </div>
    <SolidJs.Show
      when={showCounter()}
      fallback={"Counter is hidden"}
    >
      {param => <Counter.make
        initialCount={10}
      />}
    </SolidJs.Show>
    <hr />
    <h2>
      {"List Example"}
    </h2>
    <SolidJSX.For.make
      each_={match$1[0]()}
      fallback_={null}
    >
      {(item, _i) => <div>
        {item}
      </div>}
    </SolidJSX.For.make>
    <hr />
    <h2>
      {"Reactive Count"}
    </h2>
    <p>
      {"This demonstrates Solid's fine-grained reactivity"}
    </p>
    <hr />
    <Features.make />
    <hr />
    <CheckoutOptimistic.make />
  </div>;
}

let Show;

let For;

let make = App;

export {
  Show,
  For,
  make,
}
/* Counter Not a pure module */
