{
  "name": "rescript-solid-monorepo",
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
